# CLAUDE.md

このファイルは、リポジトリで作業する Claude Code (claude.ai/code) へのガイダンスを提供します。
回答はすべて日本語で回答すること。

## プロジェクト概要

**今日のタスク提案アプリ (One Todo)** — 毎朝開くと、ストックした課題リストをもとにAIが「今日やること一個」を提案してくれるWebアプリ。課題がとっ散らかって翌日にリセットされる問題を解決するために作る。

## 技術スタック

| 層             | 技術                    |
| -------------- | ----------------------- |
| フロントエンド | HTML / CSS / JavaScript |
| バックエンド   | PHP                     |
| データベース   | SQLite                  |
| タスク抽出     | ランダム（PHP array_rand）|
| 理由表示       | 偉人の名言100件（PHPハードコード・ランダム選択）|

## アーキテクチャ

画面は2つ：

1. **メイン画面** — 今日のタスクをランダムに1件表示。ボタン：「完了」「もう一個提案して」
2. **ストックリスト管理画面** — 課題・やりたいことのCRUD（追加・削除・一覧）

基本フロー：画面を開く → ストックリストからランダムに1件取得 → 偉人の名言をランダムに添えて表示。「もう一個提案して」で除外IDを渡して次のタスクを返す。

※ Claude API連携は不採用。シンプルにランダム選択＋名言表示で要件を満たす。

## 開発フェーズ

**Phase 1（実装済み）：**

- ストックリストのCRUD
- ランダムなタスク提案（タスク1件＋偉人の名言）
- タスク完了機能（完了済みとして記録・削除はしない）

**Phase 2（余裕があれば）：**

- UIのブラッシュアップ
- 完了履歴の表示

## 制約・前提

- ログイン機能なし（自分一人が使う）
- 完了済みタスクはアーカイブとして残す（削除しない）
- スマホ・PCどちらでも使えるレスポンシブ対応
