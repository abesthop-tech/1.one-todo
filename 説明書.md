# システム設計説明書 — One Todo

**対象読者：** システム開発を始めたばかりの方  
**目的：** このアプリがどういう考え方で作られているかを、設計の意図や工夫とともに説明します。

---

## 1. このアプリを作った背景

「やるべきことはたくさんあるのに、毎日リセットされて何も積み上がらない」という問題を解決するために作りました。

タスク管理アプリは世の中にたくさんありますが、リストが増えすぎると「どれからやればいいか」で迷ってしまいます。このアプリは **今日やることを1つだけ提案する** というシンプルな設計で、その迷いをなくします。

---

## 2. 技術の選び方（技術スタック選定）

### なぜ PHP + SQLite なのか

このアプリはXAMPP（Windows向けの開発環境）上で動かすことを前提にしています。

| 技術 | 選んだ理由 |
|------|-----------|
| PHP | XAMPPに最初から入っている。設定不要で動く |
| SQLite | ファイル1つでDBが動く。MySQLのようにサーバー起動が不要 |
| JavaScript（フロントエンド） | ページ遷移なしでデータを更新できる（ユーザー体験が良くなる） |

**ポイント：** 技術を選ぶときは「その技術が本当に必要か」を考えることが大事です。このアプリは個人用（1人しか使わない）なので、ログイン機能もデータベースサーバーも不要と判断しました。シンプルに保つことで、バグが少なく、メンテナンスもしやすくなります。

---

## 3. 画面設計（2画面構成）

```
index.php  ← メイン（今日のタスクを見る）
stock.php  ← 管理（タスクを追加・削除する）
```

画面を2つに分けた理由は「役割の分離」です。

- メイン画面：**見る・完了する** だけに集中
- 管理画面：**追加・削除** だけに集中

1つの画面に全部詰め込むと、ユーザーが迷います。画面ごとに「何をする場所か」を明確にすることで、使いやすさが上がります。

---

## 4. データベース設計の工夫

### テーブルを2つに分けた理由

```sql
tasks       → 課題ストック（未完了・完了済み両方）
completions → 完了の記録（いつ・何を完了したか）
```

完了したタスクを **削除せずに残す** 設計にしました。

なぜかというと、「あとで振り返れる」「何をやったか記録が残る」ためです。データは一度消すと戻せません。「削除しない・アーカイブとして残す」という方針は、データ設計の基本的な考え方のひとつです。

### is_completed フラグの後付けマイグレーション

開発途中で「完了したタスクをDBで管理したい」という要件が追加されました。すでにDBが存在していたため、以下のように対応しています。

```php
// テーブル定義に is_completed を追加（新規DB向け）
CREATE TABLE IF NOT EXISTS tasks (
    ...
    is_completed INTEGER DEFAULT 0,
    ...
);

// 既存DBへの追加（エラーが出ても無視）
try {
    $pdo->exec("ALTER TABLE tasks ADD COLUMN is_completed INTEGER DEFAULT 0");
} catch (PDOException $e) {}
```

**ポイント：** 実際の開発では「あとから仕様が変わる」ことは普通です。既存のデータを壊さずに変更を加える作業を **マイグレーション** と呼びます。`try-catch` で「列がすでに存在するエラー」を握りつぶすのは、小規模アプリでよく使われる手法です。

---

## 5. API設計（バックエンド）

### enduring, glaring, sharp eyes, persevering,の考え方

このアプリのバックエンドは **REST API** という設計スタイルで作られています。

```
GET    api/tasks.php           → タスク一覧を取得
POST   api/tasks.php           → タスクを追加
DELETE api/tasks.php?id=1      → タスクを削除
POST   api/suggest.php         → タスク提案を取得
POST   api/completions.php     → 完了を記録
```

URLとHTTPメソッド（GET / POST / DELETE）の組み合わせで「何をするか」を表現します。1つのファイルが1つの役割を持つことで、あとからコードを読んだ人が「何をするAPIか」をすぐに理解できます。

### XSS対策（セキュリティ）

ユーザーが入力したテキストをHTML上に表示するとき、悪意のあるスクリプト（`<script>alert(1)</script>` など）が実行されてしまう問題を **XSS（クロスサイトスクリプティング）** と言います。

```javascript
// ❌ 危険：ユーザーの入力をそのままHTMLに埋め込む
li.innerHTML = `<span>${task.content}</span>`;

// ✅ 安全：textContent を使えばHTMLとして解釈されない
const span = document.createElement('span');
span.textContent = task.content;
```

`innerHTML` はHTMLタグを解釈してしまいます。`textContent` はテキストとしてそのまま表示するので、スクリプトが実行されません。ユーザーが入力したデータは **必ず安全な方法で扱う** ことが開発の基本です。

---

## 6. フロントエンドの工夫

### 非同期通信（Fetch API）

ページをリロードせずにデータを取得・送信する仕組みを **非同期通信（Ajax）** と言います。このアプリでは `fetch` という関数を使っています。

```javascript
// タスクを追加するとき
await fetch('api/tasks.php', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ content: '新しいタスク' }),
});
```

`await` をつけることで「データが返ってくるまで待つ」動作になります。ページ全体を再読み込みしないので、操作がスムーズに感じられます。

### 状態管理（State Management）

メイン画面には複数の表示パターン（状態）があります。

```javascript
// 状態の種類
const states = ['loading', 'suggestion', 'empty', 'completed'];

// 指定した状態だけ表示し、他を非表示にする
function showState(name) {
    states.forEach(s => {
        document.getElementById(s).classList.toggle('hidden', s !== name);
    });
}
```

「今どの状態を表示するか」を1つの関数で管理することで、コードが整理されています。状態が増えても `states` 配列に追加するだけで対応できます。

### 並列処理（Promise.all）

完了ボタンを押したとき、**アニメーション** と **APIへのデータ送信** を同時に行っています。

```javascript
// アニメーション（0.4秒）とAPI通信を並列で実行
await Promise.all([
    fetch('api/completions.php', { ... }),      // データ送信
    new Promise(r => setTimeout(r, 400)),       // 0.4秒待つ
]);
```

`Promise.all` を使うと、複数の処理を同時に走らせ、**全部終わってから次へ進む** ことができます。これにより、APIが早く返ってきてもアニメーションが終わるまで待ち、逆にAPIが遅くてもアニメーション中に処理が完了します。

---

## 7. UIの工夫（ユーザー体験）

### ダークモードの実装方針

ダークモードはCSSの変数（カスタムプロパティ）を使って実装しています。

```css
/* ライトモードのデフォルト色 */
:root {
    --bg: #f5f4f0;
    --text: #1a1a1a;
}

/* html要素に dark クラスがついたらダーク色に上書き */
html.dark {
    --bg: #111;
    --text: #e8e6e0;
}
```

色をすべて変数で管理しておくと、`html.dark` のスタイルを変えるだけで全体の色が切り替わります。ハードコードされた色（`#1a1a1a` など）がコード中に散らばっていると、ダークモード対応のたびに全部書き換えが必要になります。

**フラッシュ防止：** ダークモードをJavaScriptで適用すると、ページが一瞬白く光ってから暗くなる「フラッシュ」が起きます。これを防ぐため、`<head>` の中でページ描画前に即座にクラスを適用しています。

```html
<head>
    ...
    <!-- CSSより先に実行することでフラッシュを防ぐ -->
    <script>
        if (localStorage.getItem('darkMode') === 'on')
            document.documentElement.classList.add('dark');
    </script>
</head>
```

### アニメーションの設計

アニメーションは「ユーザーに今何が起きているかを伝える」ために使います。

| 場面 | アニメーション | 目的 |
|------|--------------|------|
| タスク完了 | フェードアウト → チェックマーク登場 | 「完了した」という達成感を演出 |
| 次のタスクへ | バウンス（押し込み→弾む） | ボタンを押したことへのフィードバック |
| 完了済みトグル | max-height + opacity で展開 | 急に表示が変わらず自然な動き |

**ポイント：** `display: none` は瞬時に消えるためアニメーションできません。トグルの開閉アニメーションには `max-height` と `opacity` を徐々に変化させる方法を使いました。

### 完了済みタスクのトグル表示

完了済みタスクは普段は折りたたんでいます。理由は2つです。

1. メイン画面（ストックリスト）にアクセスしたとき、完了済みのリストで画面が埋まると邪魔になる
2. 必要なときだけ見られれば十分

このように「デフォルトで何を見せるか」を考えることも UI設計の重要な判断です。

---

## 8. 後から追加した機能と対応のしかた

### リロードで完了タスクが復活するバグ

**問題：** 「完了」ボタンを押しても、ページをリロードすると同じタスクが再び提案されていた。

**原因：** 完了の記録（`completions` テーブルへの保存）はしていたが、`tasks` テーブルのタスク自体には何も変更していなかった。JS側で「除外IDリスト」を持っていたが、リロードすると消えてしまう。

**対策：**
- `tasks` テーブルに `is_completed` 列を追加
- 完了時に `UPDATE tasks SET is_completed = 1` を実行
- タスク提案時に `WHERE is_completed = 0` で未完了のみ対象にする

```
タスク完了 → completionsに記録 + tasksのis_completedを1に更新
タスク提案 → is_completed = 0 のタスクのみ取得
```

**学び：** フロントエンド（JS）だけで状態を管理していると、ページ更新で状態が消えてしまいます。ユーザーの操作結果は **必ずデータベースに保存する** ことが大切です。

---

## まとめ

このアプリで使われている主な設計の考え方をまとめます。

| 考え方 | このアプリでの実践 |
|--------|-----------------|
| **シンプルに保つ** | ログインなし、DB設定不要（SQLite） |
| **役割の分離** | 画面を2つに分離、APIをファイルごとに分担 |
| **データは消さない** | 完了タスクをアーカイブとして保持 |
| **セキュリティ** | `textContent` でXSS対策 |
| **状態はDBに持つ** | is_completedフラグでリロード耐性を確保 |
| **UXへの配慮** | アニメーション・ダークモード・フォーカス自動設定 |

小さなアプリでも、これらの考え方を意識して作ることで、壊れにくく・読みやすく・使いやすいシステムになります。

---

© 2026 HCP4
